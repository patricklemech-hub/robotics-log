student = ["John", "Alice", "Bob", "Diana", "Eve"]

#print student from the list
for i in range(len(student)): #len is a syntax to get the length of the list, 
    #in this for loop, we are using the length of the list to iterate through the list, meaning that the number of loops that need to be execute is equal the lenght of the lists
    print(i, student[i]) #This command will print the student names, with the index number of the student in the list. The index number is the position of the student in the list, starting from 0. For example, John is at index 0, Alice is at index 1, and so on.
    
  
